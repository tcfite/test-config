# ===================================================================
# Spring Boot configuration for the "cloudfoundry" profile.
#
# This configuration overrides the application.yml file.
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

server:
    port: 8080
eureka:
    instance:
        prefer-ip-address: false
        hostname: ${vcap.application.uris[0]}
        nonSecurePort: 80
logging:
    level:
        com.bmcd.asset.health.uaa: DEBUG
        io.github.jhipster: DEBUG
spring:
    data:
        jest:
             uri: https://cloudfoundry:3501689820b8c9230b65cb14a41eb48d@thorin-us-east-1.searchly.com
    datasource:
        url:    jdbc:postgresql://unileverstage.cb6vzxzeeppe.eu-west-1.rds.amazonaws.com:5432/assethealth?ssl=true&sslfactory=org.postgresql.ssl.NonValidatingFactory
        username: assethealth
        password: 
liquibase:
        contexts: demo
jhipster:
    registry:
        password: GoshawkRules!
